import { Typography } from "@mui/material";

const NoRecipe = () => {
    return(
        <Typography>
            Sorry, we coudn't retrieve the data for this recipe, 
            please feel free to search for other recipes.
        </Typography> 
    )
}

export default NoRecipe;